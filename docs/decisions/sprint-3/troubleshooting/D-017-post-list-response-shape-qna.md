# D-017 게시글 목록 응답 shape Q&A

## Final Accepted Prompt

### 통과된 Decision

D-017. 게시글 목록 응답 shape를 어떻게 둘 것인가?

### 사용자 최종 답변

사용자는 선택지 B를 선택했다.

> 스프린트 일관성을 위해서 있어야 할 부분이고 추후 확장이 용이해짐

보완 답변:

> 맞아

### Codex 평가

Final Evaluation: Pass

Pass 이유:
- 사용자가 선택지 B를 명확히 골랐다.
- 페이징과 검색 결과 없음 상태를 위해 metadata가 필요하다는 점을 이해했다.
- 기존 `GET /posts` 응답이 배열에서 object로 바뀌는 개발 비용을 감수했다.
- 응답 데이터 추가와 테스트 검증 필요성을 언급했다.
- 검색 query 방식과 프론트엔드 상태 위치를 후속 Level 2 결정으로 남겼다.

보완 질문 여부:
- 질문함.
- 빈 항목이던 후속 분기를 확인했다.

### 아직 남은 후속 분기

- 검색 query 이름과 방식: `q` query param + 제목/본문 `LIKE` 검색으로 시작한다.
- 프론트엔드 검색/태그/페이징 상태 위치: React local state로 시작한다.

### 최종 결론

D-017은 통과되었다.
Sprint 3 구현 전 필수 Level 3 결정은 모두 통과되었고, 남은 검색/프론트 상태 결정은 Level 2로 낮춘다.
