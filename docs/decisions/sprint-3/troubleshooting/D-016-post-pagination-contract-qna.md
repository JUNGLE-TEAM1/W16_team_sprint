# D-016 게시글 목록 페이징 계약 Q&A

## Final Accepted Prompt

### 통과된 Decision

D-016. 게시글 목록 페이징 계약을 어떻게 둘 것인가?

### 사용자 최종 답변

사용자는 선택지 A를 선택했다.

> 확장성 메리트도 장단점이 명확해서 나중에 리팩토링한다 생각하고 좀더 직관적인 방식을 택함

보완 답변:

> 맞아
> 맞아

### Codex 평가

Final Evaluation: Pass

Pass 이유:
- 사용자가 선택지 A를 명확히 골랐다.
- `page` / `size`의 직관성을 우선하고, 추가 기능 확장성 trade-off를 감수한다고 밝혔다.
- DB/repository는 `page`와 `size`를 `offset`과 `limit`으로 변환한다고 확인했다.
- API는 `GET /api/v1/posts?page=1&size=10` 방식으로 진행한다고 확인했다.
- 테스트는 페이지별 결과 개수, 페이지 분리, 태그/검색 결합 동작을 확인한다고 정리했다.

보완 질문 여부:
- 질문함.
- 빈 항목이던 DB/API/테스트 영향과 후속 분기를 확인했다.

### 아직 남은 후속 분기

- D-017. 게시글 목록 응답 shape를 어떻게 둘 것인가?
- 검색은 제목/본문 `LIKE` 검색으로 시작할 것인가?
- 프론트엔드 목록 화면에서 검색/태그/페이징 상태를 URL query에 둘 것인가?

### 최종 결론

D-016은 통과되었지만 Sprint 3 구현 전 후속 필수 분기가 남아 있다.
다음으로 D-017을 진행한다.
