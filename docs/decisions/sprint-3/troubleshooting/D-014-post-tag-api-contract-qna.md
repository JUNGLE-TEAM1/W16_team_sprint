# D-014 게시글 태그 입력 계약 Q&A

## Final Accepted Prompt

### 통과된 Decision

D-014. 게시글 작성/수정 API에서 태그 입력 계약을 어떻게 둘 것인가?

### 사용자 최종 답변

사용자는 선택지 A를 선택했다.

> 문자열을 분리 해놓으면 배열 형식으로 프론트 엔드 조작이 편함

보완 답변:

> 맞아, 사실 두 방식의 시간복잡도는 요청과 응답의 차이이고 통틀어서 보면 차이가 없지. 하지만 나중에 기능이 많아지면 db조작이 편리해져.
> 맞아

### Codex 평가

Final Evaluation: Pass

Pass 이유:
- 사용자가 선택지 A를 명확히 골랐다.
- `tags: string[]`가 프론트엔드 조작에 유리하다는 이유를 제시했다.
- 프론트엔드 입력 변환, 백엔드 schema/validation/test 변경 비용을 감수한다고 확인했다.
- 이번 결정이 태그 필터 방식, 페이징 계약, 목록 응답 shape를 자동 확정하지 않는다는 점을 확인했다.

보완 질문 여부:
- 질문함.
- D-014의 실제 trade-off가 DB 분리보다 입력 변환과 schema/test 변경 비용이라는 점을 확인했다.
- 후속 분기를 다음 Decision으로 넘길지 확인했다.

### 아직 남은 후속 분기

- D-015. 태그로 게시글을 조회하는 API 계약을 어떻게 둘 것인가?
- 페이징 계약은 `page/size`로 둘 것인가, `limit/offset`으로 둘 것인가?
- 게시글 목록 응답 shape를 list에서 metadata 포함 object로 바꿀 것인가?
- 검색은 제목/본문 `LIKE` 검색으로 시작할 것인가?
- 프론트엔드 목록 화면에서 검색/태그/페이징 상태를 URL query에 둘 것인가?

### 최종 결론

D-014는 통과되었지만 Sprint 3 구현 전 후속 필수 분기가 남아 있다.
다음으로 D-015를 진행한다.
