# D-013 태그 모델 구조 Q&A

## Final Accepted Prompt

### 통과된 Decision

D-013. 태그 모델을 어떤 구조로 저장할 것인가?

### 사용자 최종 답변

사용자는 선택지 A를 선택했다.

> 태그 기반 검색을 할 수 있고, 분석도 가능하며 DB도 후반 확장이 용이하다.

보완 답변:

> 맞아 그래도 정규화를 하면 나중에는 편해지니까
> 다음 분기로 넘겨

### Codex 평가

Final Evaluation: Pass

Pass 이유:
- 사용자가 선택지 A를 명확히 골랐다.
- 정규화 모델의 후반 확장성과 태그 기반 검색/분석 가치를 이해했다.
- 테이블과 관계 처리 코드, create/update 테스트가 늘어나는 비용을 감수한다고 확인했다.
- 이번 결정이 API 입력 계약, 태그 필터 방식, 페이징 응답 shape를 자동 확정하지 않는다는 점을 확인했다.

보완 질문 여부:
- 질문함.
- `trade-off: 무`라고 답한 부분을 보완하기 위해 정규화 모델의 구현 비용을 감수하는지 확인했다.
- 후속 분기를 다음 Decision으로 넘길지 확인했다.

### 아직 남은 후속 분기

- D-014. 게시글 작성/수정 API에서 태그 입력 계약을 어떻게 둘 것인가?
- 태그 조회 API는 전용 endpoint로 둘 것인가, 게시글 목록 필터 query로 둘 것인가?
- 페이징 계약은 `page/size`로 둘 것인가, `limit/offset`으로 둘 것인가?
- 게시글 목록 응답 shape를 list에서 metadata 포함 object로 바꿀 것인가?
- 검색은 제목/본문 `LIKE` 검색으로 시작할 것인가?
- 프론트엔드 목록 화면에서 검색/태그/페이징 상태를 URL query에 둘 것인가?

### 최종 결론

D-013은 통과되었지만 Sprint 3 구현 전 후속 필수 분기가 남아 있다.
다음으로 D-014를 진행한다.
