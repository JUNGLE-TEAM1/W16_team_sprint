# 개발 순서 문서

## Sprint 0. 스코프 고정

### 목표

프로젝트 범위를 고정하고, 팀 전체가 같은 방향으로 구현을 시작한다.

### 결정 사항

1. 모두 같은 서비스 컨셉으로 진행한다.
2. 모두 같은 AI 기능 시나리오로 진행한다.
3. 인증 방식은 JWT로 고정한다.
4. OAuth2, Session 심화 구현은 제외한다.
5. 각자 개인 repository에 구현한다.
6. 팀 싱크는 코드 전체 리뷰가 아니라 구현 흐름과 발표 설명 중심으로 진행한다.

## Sprint 1. 인증 구현

### 목표

회원가입, 로그인, 현재 사용자 확인, 보호된 API를 구현한다.

### 구현 범위

- 회원가입
- 로그인
- JWT 발급
- 프론트엔드 token 저장
- 보호된 API 요청 시 token 첨부
- 백엔드에서 current_user 확인
- 인증 실패 처리
- 권한 실패 처리

### 완료 기준

- 회원가입이 가능하다.
- 로그인이 가능하다.
- 로그인 성공 시 JWT가 발급된다.
- 프론트엔드에 token이 저장된다.
- 게시글 작성 API 요청 시 token이 함께 전달된다.
- 백엔드에서 현재 사용자를 확인할 수 있다.
- 로그인하지 않은 사용자는 401 응답을 받는다.
- 다른 사용자의 글을 수정/삭제하려 하면 403 응답을 받는다.

### 팀 싱크 체크 포인트

- 토큰은 어디서 만들어지는가?
- 토큰에는 무엇이 들어가는가?
- 프론트엔드는 토큰을 어디에 저장하는가?
- 백엔드는 토큰을 어디서 검증하는가?
- 현재 사용자는 어떤 방식으로 API에 전달되는가?
- 401과 403은 어떻게 구분하는가?

## Sprint 2. 게시글 CRUD + 댓글

### 목표

기본 게시판 기능을 완성한다.

### 구현 범위

- 게시글 작성
- 게시글 목록 조회
- 게시글 상세 조회
- 게시글 수정
- 게시글 삭제
- 댓글 작성
- 댓글 조회
- 댓글 삭제

### 완료 기준

- 로그인한 사용자가 게시글을 작성할 수 있다.
- 게시글 목록을 조회할 수 있다.
- 게시글 상세 화면을 조회할 수 있다.
- 작성자만 게시글을 수정/삭제할 수 있다.
- 댓글을 작성할 수 있다.
- 게시글 상세 화면에서 댓글을 조회할 수 있다.

### 최소 화면 구성

- 로그인 페이지
- 게시글 목록
- 게시글 작성
- 게시글 상세
- 댓글 입력

## Sprint 3. 태그 + 페이징 + 검색

### 목표

게시판 사용성을 위한 기본 부가 기능을 구현한다.

### 구현 범위

- 태그
- 페이징
- 검색

### 완료 기준

- 글 작성 시 태그를 추가할 수 있다.
- 태그로 게시글을 조회할 수 있다.
- 게시글 목록에 페이징이 적용된다.
- 검색어로 제목/본문을 검색할 수 있다.
- 프론트엔드에서 검색과 페이징을 사용할 수 있다.

### Sprint 3 종료 기준

- 회원가입/로그인이 가능하다.
- 게시글 CRUD가 가능하다.
- 댓글 기능이 동작한다.
- 태그 기능이 동작한다.
- 검색 기능이 동작한다.
- 페이징 기능이 동작한다.

우선순위는 완성도보다 작동 여부다.

## Sprint 4. RAG 구현

### 목표

게시판 데이터와 연결된 RAG 기능을 구현한다.

### 추천 기능

사용자가 글을 작성할 때 기존 게시글 중 유사한 글을 찾아 추천하고 요약한다.

### 구현 흐름

1. 게시글 저장 시 제목/본문을 embedding한다.
2. embedding 결과를 vector DB에 저장한다.
3. 글 작성 화면에서 유사 글 찾기 기능을 제공한다.
4. 입력 중인 제목/본문을 embedding한다.
5. vector search로 유사 게시글을 검색한다.
6. 검색 결과 top 3개를 출력한다.
7. LLM이 검색 결과를 요약한다.
8. 프론트엔드에서 추천 결과를 보여준다.

### 완료 기준

- 게시글 제목/본문을 embedding한다.
- vector DB에 embedding 결과를 저장한다.
- 새 글 작성 내용과 유사한 게시글을 검색한다.
- 유사 게시글 top 3개를 보여준다.
- LLM이 검색 결과를 요약한다.
- 프론트엔드에서 추천 결과를 확인할 수 있다.

### 제외 범위

- 고급 chunking
- 복잡한 reranking
- 고급 평가 지표
- 권한별 retrieval
- 실시간 streaming
- 고급 vector DB 튜닝

Sprint 4의 핵심은 RAG가 실제 게시판 데이터와 연결되어 작동하는 것이다.

## Sprint 5. MCP 구현

### 목표

외부 서비스를 호출하는 MCP Server를 구현하고 게시판 기능과 연결한다.

### 요구사항

- MCP Server 구현
- JSON-RPC 기반 요청/응답
- 외부 서비스 1개 이상 연동
- API Key 또는 권한 관리 전략 설명

### 추천 기능

URL 메타데이터 또는 본문 요약 가져오기

### 사용자 흐름

1. 글 작성 화면에서 URL을 입력한다.
2. MCP tool을 호출한다.
3. 외부 페이지 제목, 요약, 링크를 가져온다.
4. 결과를 게시글 내용에 참고자료로 삽입한다.

### 완료 기준

- MCP Server가 존재한다.
- JSON-RPC 요청/응답 형태가 구현되어 있다.
- 외부 서비스 하나를 실제 호출한다.
- API key 또는 권한 관리 방식을 README에 설명한다.
- 외부 호출 실패 시 에러 처리가 있다.
- 프론트엔드에서 결과를 확인할 수 있다.

### 발표 설명 포인트

MCP는 LLM 또는 애플리케이션이 외부 시스템을 도구처럼 호출하기 위한 인터페이스다.
이번 프로젝트에서는 MCP Server에 외부 정보 조회 tool을 만들었다.
클라이언트 또는 백엔드가 JSON-RPC 형식으로 tool을 호출하면, MCP Server가 외부 API를 호출하고 결과를 반환한다.
이 결과를 게시글 작성에 활용한다.

## Sprint 6. Agent 구현

### 목표

RAG와 MCP를 활용하는 글쓰기 도우미 Agent를 구현한다.

### 추천 기능

사용자가 주제를 입력하면 Agent가 유사 게시글을 찾고, 외부 자료를 가져오고, 게시글 초안과 태그를 제안한다.

### 사용자 흐름

사용자 입력 예시:

```text
JWT 인증 방식에 대한 글을 쓰고 싶어
```

Agent 실행 흐름:

1. RAG tool 호출
2. 기존 유사 게시글 검색
3. MCP tool 호출
4. 외부 자료 조회
5. LLM 호출
6. 게시글 초안 작성
7. 태그 추천
8. 사용자에게 결과 표시

### 완료 기준

- Agent가 tool을 호출한다.
- RAG tool 또는 MCP tool 중 최소 하나 이상을 실제 사용한다.
- 가능하면 RAG와 MCP를 모두 사용한다.
- Agent state에 사용자 입력, tool 결과, 최종 응답이 남는다.
- 최대 반복 횟수 제한이 있다.
- tool 실패 시 예외 처리가 있다.
- 프론트엔드에서 실행할 수 있다.

### 발표 설명 포인트

Agent는 사용자의 요청을 받아 필요한 도구를 선택한다.
이번 구현에서는 RAG 검색 도구와 MCP 외부 정보 조회 도구를 tool로 제공했다.
Agent는 먼저 기존 게시글을 검색하고, 필요하면 외부 정보를 조회한 뒤, 그 결과를 종합해 게시글 초안과 태그를 생성한다.
무한 루프를 막기 위해 최대 실행 횟수를 제한했고, tool 호출 실패 시 사용자에게 에러 메시지를 반환하도록 했다.

## Sprint 7. 통합 및 버그 수정

### 목표

전체 데모 흐름이 한 번에 이어지도록 통합한다.

### 데모 흐름

1. 회원가입
2. 로그인
3. 게시글 작성
4. 댓글 작성
5. 태그/검색/페이징 확인
6. 글 작성 화면에서 RAG 유사글 추천
7. URL 입력 후 MCP 외부 정보 가져오기
8. Agent 실행
9. Agent가 초안/태그 추천

### 완료 기준

- 주요 기능이 하나의 사용자 흐름으로 연결된다.
- 데모 중 기능 간 이동이 끊기지 않는다.
- 에러가 발생할 수 있는 구간에 기본 예외 처리가 되어 있다.
- 발표자가 전체 흐름을 설명할 수 있다.

## Sprint 8. README 및 발표 준비

### 목표

구현을 마무리하고 발표 자료와 문서를 완성한다.

### 작업 범위

- README 작성
- 스크린샷 확보
- 아키텍처 그림 작성
- 발표 시나리오 작성
- 데모 리허설

### README 필수 항목

1. 프로젝트 개요
2. 주요 구현 기능
3. 전체 아키텍처 구조
4. RAG 기능 구조
5. MCP 기능 구조
6. Agent 기능 구조
7. 데모 스크린샷
8. 회고
9. 한계점
10. 개선 아이디어

### 발표 방향

발표는 코드 설명 중심이 아니라 사용자 흐름과 아키텍처 설명 중심으로 진행한다.

중점적으로 설명할 내용:

- 사용자가 어떤 문제를 해결할 수 있는가
- 게시판 기본 기능이 어떻게 동작하는가
- RAG가 게시판 데이터와 어떻게 연결되는가
- MCP가 외부 정보를 어떻게 가져오는가
- Agent가 RAG와 MCP를 어떻게 활용하는가
- 전체 데모 흐름이 어떻게 이어지는가
